# Jarvis – Voice-Enabled AI Virtual Assistant

A Python-based voice-enabled AI assistant that listens to spoken commands 
and responds with speech. It can perform tasks like web automation, 
play music, fetch real-time news, and answer general queries using an LLM API.

## Features
- Voice command recognition (SpeechRecognition)
- Text-to-speech responses (pyttsx3)
- Web automation: Google, YouTube, LinkedIn
- Music playback via custom library
- Real-time news fetching via News API
- AI-generated responses using OpenAI API

## Technologies Used
Python, SpeechRecognition, pyttsx3, REST APIs, OpenAI API, requests, GitHub

## How to Run
1. Install dependencies:
pip install -r requirements.txt

markdown
Copy code
2. Run the assistant:
python app.py

nginx
Copy code

## Wake Word
Say **"Hello"** to activate Jarvis.